# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'itsImportant' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'BigInt', '~> 2.1'
  # Pods for itsImportant

  target 'itsImportantTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'itsImportantUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
